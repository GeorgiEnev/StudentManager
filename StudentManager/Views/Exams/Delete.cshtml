@model StudentManager.Models.Exam
@{
    ViewBag.Title = "Delete Exam";
}

<h2>Delete Exam</h2>

<h3>Are you sure?</h3>
<div>
    <h4>Exam</h4><hr />
    <dl class="dl-horizontal">
        <dt>@Html.DisplayNameFor(m => m.ExamDate)</dt>
        <dd>@Html.DisplayFor(m => m.ExamDate)</dd>

        <dt>@Html.DisplayNameFor(m => m.Location)</dt>
        <dd>@Html.DisplayFor(m => m.Location)</dd>

        <dt>@Html.DisplayNameFor(m => m.Subject.Name)</dt>
        <dd>@Html.DisplayFor(m => m.Subject.Name)</dd>
    </dl>

    @using (Html.BeginForm("Delete", "Exams", FormMethod.Post))
    {
        @Html.AntiForgeryToken()
        @Html.HiddenFor(m => m.Id)
        <input type="submit" value="Delete" class="btn btn-danger" />
        @Html.ActionLink("Back to List", "Index", null, new { @class = "btn btn-default" })
    }
</div>
